using HarmonyLib;
using MelonLoader;

namespace InventoryExpansion.Patches
{
	/// <summary>
	/// Example Harmony patch. Replace this with your actual patches.
	/// 
	/// To create a patch:
	/// 1. Find the class and method you want to patch
	/// 2. Use [HarmonyPatch(typeof(ClassName), "MethodName")]
	/// 3. Create Prefix/Postfix/Transpiler methods
	/// 4. Use __instance to access the instance, __result for return values
	/// </summary>
	// [HarmonyPatch(typeof(SomeGameClass), "SomeMethod")]
	// internal static class ExamplePatch
	// {
	//     private static void Postfix(SomeGameClass __instance)
	//     {
	//         // Your patch code here
	//         MelonLogger.Msg("Example patch executed!");
	//     }
	// }
}

